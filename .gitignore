# Save data is per-table, not part of the project
admin/saves/*.json
netrunner/saves/*.json
__pycache__/
*.pyc

# Local Claude Code config
.claude/

# Exported architectures are yours to keep, but not part of the project
admin/library/*.json

# Your own rules wording is yours
admin/rules.json

# The rulebook stays on your machine. Never commit or push it -- your own copy
# is your business, but a copy in a repo is distribution.
*.pdf
*.epub
Cyberpunk_Red.pdf
